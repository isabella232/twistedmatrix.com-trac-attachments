121a122,126
>         # Begin of full-URI patch
>         if self.path[0] != '/': # if URI contains scheme and domain
>            fragments = string.split(self.path, '/')
>            self.path = "/%s" % string.join(fragments[3:], '/') # remove them
>         # End of full-URI patch
128d132
< 
